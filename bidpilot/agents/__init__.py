"""The agent roster (PRD §6.3). Each agent = a role-encoding system prompt,
a restricted toolset, and a structured output schema. No agent-to-agent open
chat — all coordination happens via ProposalState."""
